# Start Architecture Prompt

Read `AGENTS.md`, every file in `/agents`, `/docs`, and `/skills`.

Then propose the Expo architecture for CueYori.

Do not write app feature code yet.

Include:

1. folder structure
2. route structure
3. state boundaries
4. notification boundaries
5. first milestone
